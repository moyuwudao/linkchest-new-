name = "linkchest-metadata"
main = "src/index.ts"
compatibility_date = "2024-01-01"

# 使用 Cloudflare 免费套餐
# 每天 100,000 次请求限制

# KV 命名空间（用于缓存）
[[kv_namespaces]]
binding = "METADATA_CACHE"
id = "6eeabdec16e44b5a8d3e8b8b63b84eb1"
